# openmaterials: the product

The product (the free protocol and the hosted app, the map, the parsers, the
index, the contracts, governance, and build order) is now Part II of
`docs/openmaterials.pdf` (source `docs/openmaterials.tex`), the project's single
source of truth.
