# Per-skill encoding procedure

The encoding procedure now lives in Appendix C ("Encoding a catalog skill") of
`docs/openmaterials.pdf` (source `docs/openmaterials.tex`), the project's single
source of truth.
