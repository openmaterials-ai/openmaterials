# Skills

The two operator-layer procedures that used to live here are now appendices of
`docs/openmaterials.pdf` (source `docs/openmaterials.tex`), the project's single
source of truth:

- Ingesting an external code into the representation layer: Appendix A.
- Extending the operator DAG with a new node or edge: Appendix B.
